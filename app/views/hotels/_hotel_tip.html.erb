
<%# park_name = park_listing.name %>
<%# park_name_class = 'mod ' %>
<%# column_number == 1 ? park_name_class : park_name_class += "mod channel ch-C3 "  %>
<%# last_listing == 'true' ? park_name_class  += "cls ": park_name_class += "line "  %>

    <!-- <div id="<%#= park_name %>" class="<%#= park_name_class %>">
        <div class="hd cls">
            <h2> <%#= link_to park_listing.name, district_path(park_listing) %> </h2>

        </div>
        <div class="bd">
        </div>
    </div> -->


<!-- -->
    <%#= render :partial => 'tip' %>
    <div class="comment">
      <div class="comment-image">
          <%= image_tag "#{hotel_tip.author_photo_prefix}32x32#{hotel_tip.author_photo_suffix}",
               title: "#{hotel_tip.author_first_name} #{hotel_tip.author_last_name} ", 
               class: "avatar",
               data_retina_url: "#{hotel_tip.author_photo_prefix}64x64#{hotel_tip.author_photo_suffix}",
               size: "32x64"
          %>
      </div>
      <div class="comment-content">
        <h1><%= "#{hotel_tip.author_first_name} #{hotel_tip.author_last_name}" %></h1>
        <p><%= hotel_tip.text %></p>
        <!-- <p class="comment-detail">Date or details about this post</p> -->
      </div>
    </div>

    
<!-- -->
